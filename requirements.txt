anthropic>=1.2.0

# Gemini needs no package -- providers.py calls its REST API with urllib.
# Just set GEMINI_API_KEY (or GOOGLE_API_KEY) and it is used automatically,
# or `nicomachus providers --use gemini` to prefer it when both keys are set.
